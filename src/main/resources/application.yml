server:
  port: 9101

spring:
  profiles:
    active: dev
  application:
    name: @pom.artifactId@
  boot:
    admin:
      username: admin
      password: admin

security:
  user:
    name: admin
    password: admin